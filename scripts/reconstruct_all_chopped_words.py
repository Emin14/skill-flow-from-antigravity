# -*- coding: utf-8 -*-
"""
Reconstruct all chopped Russian words by cross-referencing with parsed_index.json
and identifying the exact original Russian words from Mueller's dictionary.
"""
import json, os, sys, re

if hasattr(sys.stdout, 'reconfigure'):
    sys.stdout.reconfigure(encoding='utf-8')

oxford_path = os.path.abspath('oxford_5000_2026-08-27.json')
with open(oxford_path, 'r', encoding='utf-8') as f:
    data = json.load(f)

parsed_index = json.load(open('scripts/parsed_index.json', 'r', encoding='utf-8'))

# Map of prefix restorations based on corrupted initial patterns and false register flags:
PREFIX_RULES = [
    # (False register flag to remove / check, pattern in text, replacement prefix)
    # 1. 'просодия' -> was 'прос.' from 'просить', 'простой', etc.
    ('просодия', r'^ить,?\s*(.*)$', 'просить, \1', 'просодия'),
    ('просодия', r'^ить\b', 'просить', 'просодия'),
    ('просодия', r'^ить разрешения', 'просить разрешения', 'просодия'),
    
    # 2. 'юридическое' -> was 'юр.' from 'юридическое лицо'
    (None, r'^и\s*дическое лицо\b', 'юридическое лицо', 'юридическое'),
    
    # 3. 'жаргон' -> was 'сл.' from 'след', 'слепо', 'слабый', 'славный', 'слово', etc.
    ('жаргон', r'^ед укуса\b', 'след укуса', 'жаргон'),
    ('жаргон', r'^епо\s+напечатанный\b', 'слепо напечатанный', 'жаргон'),
    ('жаргон', r'^а\s*бый,\s*тонкий\b', 'слабый, тонкий', 'жаргон'),
    ('жаргон', r'^ав\s*ный\s*малый\b', 'славный малый', 'жаргон'),
    ('жаргон', r'^у\s*жебное\s*слово\b', 'служебное слово', 'жаргон'),
    
    # 4. 'ветеринария' -> was 'вет.' from 'ветка'
    ('ветеринария', r'^ка\s*плюща\b', 'ветка плюща', 'ветеринария'),
    
    # 5. 'собирательное' -> was 'соб.' from 'собирать', 'собственный', 'собака'
    ('собирательное', r'^ать\s*в\s*сборки\b', 'собирать в сборки', 'собирательное'),
    ('собирательное', r'^ать\s*\(налоги\b', 'собирать (налоги', 'собирательное'),
    ('собирательное', r'^ать\s*\(сведения\b', 'собирать (сведения', 'собирательное'),
    ('собирательное', r'^ать\s*пошлину\b', 'собирать пошлину', 'собирательное'),
    
    # 6. 'минералогия' -> was 'мин.' from 'минное', 'минировать', 'минерал'
    ('минералогия', r'^ное\s*дело;\s*минирование\b', 'минное дело; минирование', 'минералогия'),
    ('минералогия', r'^ное\s*дело\b', 'минное дело', 'минералогия'),
    ('минералогия', r'^ная\s*галерея\b', 'минная галерея', 'минералогия'),
    ('минералогия', r'^ировать;\s*подрывать\b', 'минировать; подрывать', 'минералогия'),
    ('минералогия', r'^ировать\b', 'минировать', 'минералогия'),
    
    # 7. 'морской термин' -> was 'мор.' from 'морской', 'морить'
    (None, r'^ской\s*волк\b', 'морской волк', None),
    (None, r'^ской\s*конёк\b', 'морской конёк', None),
    (None, r'^ской\s*пи\s*рат\b', 'морской пират', None),
    (None, r'^ской\s*прилив\b', 'морской прилив', None),
    (None, r'^ской\s*бой\b', 'морской бой', None),
    ('морской термин', r'^ить\s*голодом\b', 'морить голодом', 'морской термин'),
    
    # 8. 'военное' -> was 'воен.' from 'военный', 'военно-'
    (None, r'^ный,\s*военноморской\s*врач\b', 'военный, военно-морской врач', None),
    (None, r'^ная\s*служба\b', 'военная служба', None),
    
    # 9. 'устаревшее' -> was 'уст.' from 'устав', 'устанавливать', 'устный', 'устье'
    ('устаревшее', r'^ав\s*\(общества,\s*ордена\)\b', 'устав (общества, ордена)', 'устаревшее'),
    ('устаревшее', r'^а\s*навливать,\s*точно\s*определять\b', 'устанавливать, точно определять', 'устаревшее'),
    ('устаревшее', r'^ное\s*заявление,\s*признание\b', 'устное заявление, признание', 'устаревшее'),
    ('устаревшее', r'^ный\b', 'устный', 'устаревшее'),
    ('устаревшее', r'^ье\s*\(реки\)\b', 'устье (реки)', 'устаревшее'),
    ('устаревшее', r'^ро\s*итель,\s*организатор\b', 'устроитель, организатор', 'устаревшее'),
    
    # 10. 'строительное' -> was 'стр.' from 'стремиться', 'стремительно', 'стрелка', 'строгий'
    ('строительное', r'^е\s*мительно\s*нестись\b', 'стремительно нестись', 'строительное'),
    ('строительное', r'^е\s*миться\b', 'стремиться', 'строительное'),
    ('строительное', r'^ел\s*ка\b', 'стрелка', 'строительное'),
    ('строительное', r'^ого\s*и\s*пр\b', 'строгого и пр.', 'строительное'),
    
    # 11. 'искусство' -> was 'иск.' from 'искать', 'искажать', 'искровой', 'иск'
    ('искусство', r'^ать\s*в\s*суде\b', 'искать в суде', 'искусство'),
    ('искусство', r'^а\s*жать,\s*искривлять\b', 'искажать, искривлять', 'искусство'),
    ('искусство', r'^ро\s*вой\s*промежуток\b', 'искровой промежуток', 'искусство'),
    ('искусство', r'^ать\s*\(for\)\b', 'искать (for)', 'искусство'),
    
    # 12. 'топография' -> was 'топ.' from 'топить'
    ('топография', r'^ить\s*\(сало\)\b', 'топить (сало)', 'топография'),
    ('топография', r'^ить\s*\(судно\)\b', 'топить (судно)', 'топография'),
    
    # 13. 'телевидение' -> was 'тел.' from 'телевидение', 'тело'
    ('телевидение', r'^е\s*видение\b', 'телевидение', None),
    ('телевидение', r'^еви\s*дение\b', 'телевидение', None),
    ('телевидение', r'^ефонная\s*будка\b', 'телефонная будка', None),
    
    # 14. 'математика' -> was 'мат.' from 'материнский'
    ('математика', r'^е\s*ринское\s*молоко\b', 'материнское молоко', 'математика'),
    
    # 15. 'научное' -> was 'научн.' from 'научный'
    ('научное', r'^ный\b', 'научный', 'научное'),
    ('научное', r'^ная\s*работа,\s*монография\b', 'научная работа, монография', 'научное'),
    ('научное', r'^ная\s*конференция\b', 'научная конференция', 'научное'),
    
    # 16. 'театр' -> was 'театр.' from 'театральный'
    ('театр', r'^аль\s*ный,\s*сценический\b', 'театральный, сценический', None),
    ('театр', r'^аль\s*ный,\s*неестественный\b', 'театральный, неестественный', None),
    
    # 17. 'музыка' -> was 'муз.' from 'музыка'
    ('музыка', r'^ыка\s*на\s*слова\b', 'музыка на слова', None),
    
    # 18. 'религиозное' -> was 'рел.' from 'религиозный'
    ('религиозное', r'^иги\s*озный,\s*церковный\b', 'религиозный, церковный', None),
    
    # 19. 'торговля' -> was 'торг.' from 'торговать'
    ('торговля', r'^овать\s*\(in\b', 'торговать (in', 'торговля'),
    ('торговля', r'^овать\s*вразнос\b', 'торговать вразнос', 'торговля'),
    ('торговля', r'^овать\b', 'торговать', 'торговля'),
    
    # 20. 'уменьшительное' -> was 'уменьш.' from 'уменьшать'
    ('уменьшительное', r'^ать\s*глубину\b', 'уменьшать глубину', 'уменьшительное'),
    
    # 21. 'живопись' -> was 'жив.' from 'живой', 'дикий'
    ('живопись', r'^ая\s*природа\b', 'дикая природа', 'живопись'),
    
    # 22. 'горное дело' -> was 'стол.' from 'столовое'
    (None, r'^ое\s*плато,\s*плоскогорье\b', 'столовое плато, плоскогорье', None),
    
    # 23. 'историческое' -> was 'ист.' from 'исток'
    ('историческое', r'^ок\s*\(реки\)\b', 'исток (реки)', 'историческое'),
    
    # 24. 'редкое' -> was 'ред.' from 'редкий'
    ('редкое', r'^ий\s*\(о\s*волосах,\s*лесе\)\b', 'редкий (о волосах, лесе)', 'редкое'),
    
    # 25. 'медицинское' -> was 'син.' from 'синяк', 'мед.' from 'медный'
    (None, r'^ный\s*лоб,\s*тупость\b', 'медный лоб, тупость', None),
    (None, r'^як\b', 'синяк', None),
    
    # 26. Incomplete phrase patterns starting with 'или ...)'
    (None, r'^или\s+([a-zA-Z\s\',/-]+)\)\s*(.*)$', r'\2', None),
    (None, r'^или\s+([а-яА-Я\s\',/-]+)\)\s*(.*)$', r'\2', None)
]

def get_raw_text(w):
    raw_texts = []
    if w in parsed_index:
        raw_texts.extend([e.get('text', '') for e in parsed_index[w]])
    else:
        for k in parsed_index:
            if k == w or k.startswith(w + ' ') or k.startswith(w + '1') or k.startswith(w + '2'):
                raw_texts.extend([e.get('text', '') for e in parsed_index[k]])
    return '\n'.join(raw_texts)

fixed_count = 0

for item in data:
    w = item['word']
    raw = get_raw_text(w)
    
    for m in item.get('meanings', []):
        t = m.get('translation', '')
        old_t = t
        
        for reg_cond, pat, repl, reg_to_remove in PREFIX_RULES:
            if reg_cond is None or reg_cond in m.get('register', []):
                if re.search(pat, t):
                    t = re.sub(pat, repl, t)
                    if reg_to_remove and reg_to_remove in m.get('register', []):
                        m['register'].remove(reg_to_remove)
                        
        # General check: soft split inside word like "sea cow |'si:kau] n"
        t = re.sub(r'\s+[a-zA-Z-]+\s+(?:\[[^\]]+\]|\|[^\s]+\]|[1-4]\.|\bI+\b).*$', '', t)
        t = re.sub(r'\s+', ' ', t).strip(' ,;:-')
        
        if t != old_t:
            m['translation'] = t
            fixed_count += 1

    for p in item.get('phrases', []):
        t = p.get('translation', '')
        old_t = t
        
        for reg_cond, pat, repl, reg_to_remove in PREFIX_RULES:
            if reg_cond is None or reg_cond in p.get('register', []):
                if re.search(pat, t):
                    t = re.sub(pat, repl, t)
                    if reg_to_remove and reg_to_remove in p.get('register', []):
                        p['register'].remove(reg_to_remove)
                        
        t = re.sub(r'\s+', ' ', t).strip(' ,;:-')
        if t != old_t:
            p['translation'] = t
            fixed_count += 1

print(f"Total reconstructed chopped words: {fixed_count}")

with open(oxford_path, 'w', encoding='utf-8') as f:
    json.dump(data, f, ensure_ascii=False, indent=2)
    f.write('\n')

print("Dataset successfully updated with reconstructed words!")
